const displayText400 = () => {
    var elements = document.getElementsByClassName('400level');
    for (var idx = 0; idx < elements.length; idx++) {
        console.log(elements[idx].innerText);
    }
}

